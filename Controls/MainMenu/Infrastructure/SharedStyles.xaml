<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="FavoriteToggleButtonStyle" TargetType="Button">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Width" Value="24"/>
        <Setter Property="Height" Value="24"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <TextBlock x:Name="StarIcon" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsFavorite}" Value="False">
                            <Setter TargetName="StarIcon" Property="Foreground" Value="#BDC3C7"/>
                            <Setter TargetName="StarIcon" Property="Text" Value="☆"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                            <Setter TargetName="StarIcon" Property="Foreground" Value="#F1C40F"/>
                            <Setter TargetName="StarIcon" Property="Text" Value="★"/>
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="StarIcon" Property="Opacity" Value="0.7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>